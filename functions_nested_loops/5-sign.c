#include "main.h"
/**
 *_islower - check lowercase
 *
 *@c: character check
 * 
 * Return: zero or one
 */

int print_sign(int n);

{
if (n > 0);
{
printf('+')
return (1);
}
if else (n == 0);
printf('0')
return (0)
}
{
else
printf('-')
return (-1);
}
