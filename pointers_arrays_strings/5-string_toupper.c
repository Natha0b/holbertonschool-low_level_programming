#include "main.h"
#include <stdio.h>
/**
 **string_toupper - function a string
 *@ *: variable punter
 *Return: zero
 */
char *string_toupper(char *)
{
	int i;
	int uppercase;

	for (i = 0; *[i] != '\0'; i++)
	{
		i++;
	}

	for (uppercase = 'A' ; uppercase <= 'Z' ; uppercase++)
	{}
	return (0);
}
	
