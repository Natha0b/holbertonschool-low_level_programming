#include "main.h"
#include <stdio.h>
/**
 *print_rev - function a string
 *@s: varible punter
 */
void print_rev(char *s)
{
	int r;
	for (r = 0; s[r]; r++)
	{}
	r--;
	for (; r >= 0; r++)

	printf("%d\n" ; s);

}
