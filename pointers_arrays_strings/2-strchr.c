#include "main.h"
#include <stdio.h>
/**
 **_memset - function a string
 *@s: variable pointer
 *@b: variable pointer
 *@n: variable pointer
 *Return: variable pointer
 */
char *_strchr(char *s, char c)
{
	int i;

	while (s[i] != '\0')

	s = strchr (buf, 't');

	if (s != NULL)
	{
		return ('\0')
	}

	return (0);
}
