#include "main.h"
#include <stdio.h>
/**
 **_memset - function a string
 *@s: variable pointer
 *@b: variable pointer
 *@n: variable pointer
 *Return: variable pointer
 */
char *_strchr(char *s, char c)
{

	while (s[c] != '\0')


	if (s != NULL)
	{
		return ('\0');
	}

	return (s);
}
