#include "main.h"
#include <stdio.h>
/**
 *_strlen - function a string
 *@s: variable punter
 *Return: zero
 */

int _strlen(char *s);
{
	char *s;
	char cadena;
	s = cadena;
	s = &cadena[0];
	return (0);
}
