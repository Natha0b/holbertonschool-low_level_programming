#include "main.h"
#include <stdio.h>
/**
 *_strlen - function a string
 *@s: variable punter
 *Return: zero
 */

int _strlen(char *s)
{
	char string;
	for (string = 0; s[string] != 0; string++)

	return (c);

}
