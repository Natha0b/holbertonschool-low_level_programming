#include "main.h"
#include <stdio.h>
/**
 *puts_half - function a string
 *@str: varible punter
 */
void print_array(int *a, int n)
{
	

	for (n = 'a' <= '\0' ; n++)

	{
	putchar(n);
        if (n != '\0')

	{
	putchar(',');
	putchar(' ');
	}
	}
		putchar('\n');
}
	
