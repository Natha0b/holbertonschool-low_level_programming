#include <stdio.h>
#include "main.h"
#include <stdlib.h>
/**
 **_strdup- function malloc and free
 *@str: variable malloc
 *Return: zero
 */
char *_strdup(char *str)
{
    char *a;
    int i;

    for (i = 0; i < '\0'; i++)

    a = malloc(size * sizeof(char));

    if (str == NULL)
    {
        return (NULL);
    }

    return (str);
}