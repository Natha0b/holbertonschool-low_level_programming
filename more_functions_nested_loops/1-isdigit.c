#include "main.h"
#include <stdio.h>

/**
 *_isdigit - print the uppercase character
 *@c: check caracter
 *Return: zero and one
 */

int _isdigit(int c)

{
if (c >= '0' && c <= '9')

return (1);

else

return (0);
}
