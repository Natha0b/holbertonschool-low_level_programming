#include "main.h"
#include <stdio.h>

/**
 *mul - multiplies numbers
 *@a: multiplies caracter
 *@b: multiplies caracter
 *Return: result
 */

int mul(int a, int b)
{
int mul;

mul = a * b;

return (mul);

}
