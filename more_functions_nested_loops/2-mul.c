#include "main.h"
#include <stdio.h>

/**
 *mul - print the multiplies character
 *@a: check caracter
 *@b: check caracter
 *Return: zero and one
 */

int mul(int a, int b)
{

_putchar("%d\n", (a, *b));

return (0);

}
