#include "main.h"
#include <stdio.h>

/**
 *mul - print the multiplies character
 *@a: check caracter
 *@b: check caracter
 *Return: zero and one
 */

int mul(int a, int b)
{

printf("%d\n", mul(a, b))

return (0);
}
