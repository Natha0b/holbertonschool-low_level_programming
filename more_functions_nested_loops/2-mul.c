#include "main.h"
#include <stdio.h>

/**
 *mul - multiplies numbers
 *@a: multiplies caracter
 *@b: multiplies caracter
 *Return: result
 */

int mul(int a, int b)
{

mul = (a * b);
printf("mul %d and %d is %d",a,b,mul);
return (mul);

}
